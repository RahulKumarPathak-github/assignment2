// Define the arrow function
const square = (number) => number * number;

// Calculate and display the square of a given number
const givenNumber = 5; // Replace with any number you want to square
const result = square(givenNumber);

console.log(`The square of ${givenNumber} is ${result}`);